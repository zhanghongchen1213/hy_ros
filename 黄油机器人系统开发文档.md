# 黄油机器人系统开发文档

> **ROS 版本**：ROS2 Humble
> **硬件平台**：瑞芯微 RK3588 + STM32 下位机
> **核心功能**：差速底盘控制、SLAM 建图导航、视觉识别、语音交互
> **开发环境**：Ubuntu 22.04 (Linux 6.8.0-90-generic)

---

## 1. 系统架构 (System Architecture)

### 1.1 功能模块划分

黄油机器人系统采用分层架构设计，主要包含以下功能模块：

#### **感知层 (Perception Layer)**
- **激光雷达**：LDLiDAR 2D 激光雷达，提供 360° 环境扫描
- **视觉系统**：RK3588 硬件加速的 YOLO 目标检测
- **IMU 传感器**：6 轴惯性测量单元，提供姿态和角速度数据
- **轮式里程计**：基于编码器的差速轮式里程计

#### **定位与建图 (Localization & Mapping)**
- **SLAM 建图**：基于 `slam_toolbox` 的在线异步 SLAM
- **里程计融合**：使用 `robot_localization` 的 EKF 融合轮式里程计和 IMU
- **激光滤波**：使用 `laser_filters` 过滤机器人自身遮挡区域

#### **控制层 (Control Layer)**
- **底盘控制**：通过 UART 串口与 STM32 通信，控制差速驱动
- **PID 调试**：实时调整电机 PID 参数
- **多输入控制**：支持键盘、手柄 (Joy) 控制

#### **交互层 (Interaction Layer)**
- **语音识别 (ASR)**：音频流语音识别
- **大语言模型 (LLM)**：基于 RKLLM 的本地推理
- **语音合成 (TTS)**：文本转语音输出
- **远程监控**：Foxglove Bridge 提供 Web 端可视化

#### **视觉处理管道 (Vision Pipeline)**
- **相机采集**：`rk_camera` 组件，1920x1080@30fps
- **目标检测**：`rk_inference` 组件，YOLOv8 RKNN 加速推理
- **视频推流**：`rk_streamer` 组件，RTSP 流输出

### 1.2 节点关系图 (Node Graph)

```
核心数据流向：

[LDLiDAR] --/scan--> [LaserFilter] --/scan_filtered--> [SLAM_Toolbox] --/map-->
                                                              |
[STM32_UART] --/uart/wheel_raw--> [EKF_Node] <--/uart/imu_data-- [STM32_UART]
                                       |
                                  /odometry/filtered
                                       |
                                       v
                              [Navigation/Control]

[RK_Camera] --/yolo/image_raw--> [RK_Inference] --/yolo/image_infer--> [RK_Streamer] --> RTSP

[Audio_ASR] --/uart/chat_gpt_count--> [Audio_RKLLM] --> [Audio_TTS]

[Keyboard/Joy] --/joy--> [UART_Node] --/uart/motor_control--> [STM32]
```

---

## 2. 软件包清单 (Packages)

### 2.1 all_launch (系统总启动包)

- **路径**：`src/all_launch`
- **功能**：系统总启动文件，协调所有子系统的启动顺序
- **依赖**：uart, ekf_odom, audio_asr, audio_rkllm, audio_tts, butter_robot_description, foxglove_bridge, rk_camera, rk_inference, rk_streamer
- **启动文件**：`all_launch.launch.py`

### 2.2 uart (串口通信包)

- **路径**：`src/uart`
- **功能**：与 STM32 下位机进行串口通信，负责底盘控制和传感器数据上报
- **通信协议**：自定义二进制协议 (0xAA 起始, 0x55/0x66 结束)
- **波特率**：460800
- **串口设备**：`/dev/ttyS3`
- **主要功能**：
  - 接收轮式里程计数据 (`/uart/wheel_odom`)
  - 接收 IMU 数据 (`/uart/imu_data`)
  - 接收电机状态 (`/uart/motor_status`)
  - 发送电机控制指令 (`/uart/motor_control`)
  - 支持手柄控制 (`/joy`)

### 2.3 ekf_odom (里程计融合包)

- **路径**：`src/ekf_odom`
- **功能**：使用扩展卡尔曼滤波器 (EKF) 融合轮式里程计和 IMU 数据
- **依赖**：`robot_localization`
- **配置文件**：`config/ekf.yaml`
- **输入**：
  - `/uart/wheel_raw` (轮式里程计)
  - `/uart/imu_data` (IMU 数据)
- **输出**：
  - `/odometry/filtered` (融合后的里程计)
  - TF: `odom -> base_link`
- **融合策略**：
  - 线速度 100% 来自轮式里程计
  - 角速度和姿态 100% 来自 IMU
  - 位置由线速度和 IMU 朝向共同积分

### 2.4 butter_robot_description (机器人模型包)

- **路径**：`src/butter_robot_description`
- **功能**：包含机器人 URDF 模型、Mesh 文件和可视化配置
- **URDF 文件**：`urdf/butter_robot.urdf`
- **主要连杆**：
  - `base_link` (底盘基座)
  - `left_wheel_Link` / `right_wheel_Link` (驱动轮)
  - `radar_Link` (激光雷达)
  - `spine_Link` (躯干)
  - `camera_Link` (相机)
  - `left_arm_Link` / `right_arm_Link` (机械臂)

### 2.5 hy_slam (SLAM 建图包)

- **路径**：`src/hy_slam`
- **功能**：基于 `slam_toolbox` 的在线 SLAM 建图和定位
- **依赖**：`slam_toolbox`, `laser_filters`
- **配置文件**：
  - `config/mapper_params_online_async.yaml` (SLAM 参数)
  - `config/laser_filter.yaml` (激光滤波参数)
- **启动文件**：
  - `slam.launch.py` (在线建图)
  - `localization.launch.py` (定位模式)
  - `laser_filter_only.launch.py` (仅激光滤波)
- **激光滤波**：过滤机器人后方车身遮挡区域 (base_link 坐标系)

### 2.6 ldlidar_driver_ros2 (激光雷达驱动包)

- **路径**：`src/ldlidar_driver_ros2`
- **功能**：LDLiDAR 2D 激光雷达驱动
- **串口设备**：`/dev/ldlidar_serial`
- **发布话题**：`/scan` (sensor_msgs/LaserScan)
- **坐标系**：`radar_Link`
- **角度偏移**：-90° (矫正安装方向)

### 2.7 视觉处理包 (rk_camera, rk_inference, rk_streamer)

#### **rk_camera (相机采集组件)**
- **路径**：`src/rk_camera`
- **功能**：RK3588 硬件加速的相机采集
- **分辨率**：1920x1080@30fps
- **设备 ID**：0
- **图像旋转**：180° (rotation_180: true)
- **发布话题**：`/yolo/image_raw`
- **控制话题**：`/uart/enable_butter_yolo` (启用/禁用采集)

#### **rk_inference (目标检测组件)**
- **路径**：`src/rk_inference`
- **功能**：YOLOv8 RKNN 加速推理
- **模型路径**：`/opt/rknn-toolkit2-lite/yolov8.rknn`
- **订阅话题**：`/yolo/image_raw`
- **发布话题**：`/yolo/image_infer` (带检测框的图像)
- **置信度阈值**：0.5
- **NMS 阈值**：0.45

#### **rk_streamer (视频推流组件)**
- **路径**：`src/rk_streamer`
- **功能**：RTSP 视频流推送
- **订阅话题**：`/yolo/image_infer`
- **推流地址**：`rtsp://127.0.0.1:8554/camera`
- **编码格式**：H.264 (硬件加速)

**注意**：这三个组件运行在同一个 `ComposableNodeContainer` 中，启用零拷贝通信 (intra_process_comms)，大幅降低延迟和 CPU 占用。

### 2.8 音频交互包 (audio_asr, audio_rkllm, audio_tts)

#### **audio_asr (语音识别包)**
- **路径**：`src/audio_asr`
- **功能**：实时语音识别 (ASR)
- **发布话题**：`/uart/chat_gpt_count` (识别结果计数)

#### **audio_rkllm (大语言模型包)**
- **路径**：`src/audio_rkllm`
- **功能**：基于 RKLLM 的本地大语言模型推理
- **订阅话题**：`/uart/chat_gpt_count`
- **特性**：支持强制英文输出选项

#### **audio_tts (语音合成包)**
- **路径**：`src/audio_tts`
- **功能**：文本转语音 (TTS)
- **订阅话题**：来自 audio_rkllm 的文本输出

### 2.9 keyboard_control (键盘控制包)

- **路径**：`src/keyboard_control`
- **功能**：键盘遥控机器人移动
- **发布话题**：`/uart/motor_control`

### 2.10 pid_debug_interfaces (自定义消息包)

- **路径**：`src/pid_debug_interfaces`
- **功能**：定义电机控制和状态的自定义消息类型
- **消息类型**：
  - `MotorControl.msg` (电机控制指令)
  - `MotorStatus.msg` (电机状态反馈)

---

## 3. 通信接口详解 (Communication Interfaces)

### 3.1 核心话题 (Topics)

| 话题名称 | 消息类型 | 发布者 | 订阅者 | 描述 |
|---------|---------|--------|--------|------|
| `/scan` | `sensor_msgs/LaserScan` | ldlidar_driver | laser_filter | 2D 激光雷达原始数据 |
| `/scan_filtered` | `sensor_msgs/LaserScan` | laser_filter | slam_toolbox | 过滤后的激光数据 |
| `/uart/wheel_raw` | `nav_msgs/Odometry` | uart_node | ekf_filter_node | 轮式里程计原始数据 |
| `/uart/imu_data` | `sensor_msgs/Imu` | uart_node | ekf_filter_node | IMU 传感器数据 |
| `/odometry/filtered` | `nav_msgs/Odometry` | ekf_filter_node | navigation | EKF 融合后的里程计 |
| `/uart/motor_status` | `MotorStatus` | uart_node | - | 电机状态和 PID 参数 |
| `/uart/motor_control` | `MotorControl` | keyboard_control | uart_node | 电机控制指令 |
| `/joy` | `sensor_msgs/Joy` | joy_node | uart_node | 游戏手柄控制输入 |
| `/yolo/image_raw` | `sensor_msgs/Image` | rk_camera | rk_inference | 原始相机图像 (NV12) |
| `/yolo/image_infer` | `sensor_msgs/Image` | rk_inference | rk_streamer | 带检测框的图像 |
| `/uart/chat_gpt_count` | `std_msgs/UInt16` | audio_asr | audio_rkllm | 语音识别结果计数 |
| `/uart/audio_stream_flag` | `std_msgs/UInt8` | audio_* | uart_node | 音频流状态标志 |
| `/uart/enable_butter_yolo` | `std_msgs/Bool` | uart_node | rk_camera | 启用/禁用视觉检测 |

### 3.2 自定义消息 (Custom Messages)

#### **MotorControl.msg**
```
float32 left_target_speed      # 左电机目标速度 (m/s)
float32 right_target_speed     # 右电机目标速度 (m/s)
float32 left_kp                # 左电机 PID 比例系数
float32 left_ki                # 左电机 PID 积分系数
float32 left_kd                # 左电机 PID 微分系数
float32 right_kp               # 右电机 PID 比例系数
float32 right_ki               # 右电机 PID 积分系数
float32 right_kd               # 右电机 PID 微分系数
float32 linear_vel             # 线速度 (m/s)
float32 angular_vel            # 角速度 (rad/s)
float32 servo_a_angle          # 舵机 A 角度 (rad)
float32 servo_b_angle          # 舵机 B 角度 (rad)
float32 servo_c_angle          # 舵机 C 角度 (rad)
```

#### **MotorStatus.msg**
```
float32 left_target_speed      # 左电机目标速度
float32 right_target_speed     # 右电机目标速度
float32 left_actual_speed      # 左电机实际速度
float32 right_actual_speed     # 右电机实际速度
float32 left_kp                # 左电机当前 PID 参数
float32 left_ki
float32 left_kd
float32 right_kp               # 右电机当前 PID 参数
float32 right_ki
float32 right_kd
```

### 3.3 参数配置 (Parameters)

#### **EKF 里程计融合参数**

| 节点 | 参数名 | 默认值 | 说明 |
|------|--------|--------|------|
| `ekf_filter_node` | `frequency` | 50.0 | 滤波器更新频率 (Hz) |
| `ekf_filter_node` | `two_d_mode` | true | 2D 模式 (忽略 Z/Roll/Pitch) |
| `ekf_filter_node` | `sensor_timeout` | 0.1 | 传感器超时时间 (s) |
| `ekf_filter_node` | `odom0` | `/uart/wheel_raw` | 轮式里程计话题 |
| `ekf_filter_node` | `imu0` | `/uart/imu_data` | IMU 数据话题 |
| `ekf_filter_node` | `world_frame` | `odom` | 世界坐标系 |
| `ekf_filter_node` | `odom_frame` | `odom` | 里程计坐标系 |
| `ekf_filter_node` | `base_link_frame` | `base_link` | 机器人基座坐标系 |

#### **激光雷达参数**

| 节点 | 参数名 | 默认值 | 说明 |
|------|--------|--------|------|
| `LDLiDAR` | `port_name` | `/dev/ldlidar_serial` | 串口设备路径 |
| `LDLiDAR` | `frame_id` | `radar_Link` | 雷达坐标系 |
| `LDLiDAR` | `topic_name` | `/scan` | 发布话题名称 |
| `LDLiDAR` | `angle_offset` | -90.0 | 角度偏移量 (度) |
| `LDLiDAR` | `enable_angle_crop_func` | false | 是否启用角度裁剪 |

#### **视觉处理参数**

| 节点 | 参数名 | 默认值 | 说明 |
|------|--------|--------|------|
| `rk_camera` | `width` | 1920 | 图像宽度 |
| `rk_camera` | `height` | 1080 | 图像高度 |
| `rk_camera` | `framerate` | 30 | 帧率 (fps) |
| `rk_camera` | `rotation_180` | true | 图像旋转 180° |
| `rk_inference` | `conf_threshold` | 0.5 | 置信度阈值 |
| `rk_inference` | `nms_threshold` | 0.45 | NMS 阈值 |
| `rk_inference` | `model_path` | `/opt/rknn-toolkit2-lite/yolov8.rknn` | RKNN 模型路径 |

#### **串口通信参数**

| 节点 | 参数名 | 默认值 | 说明 |
|------|--------|--------|------|
| `uart` | `port` | `/dev/ttyS3` | 串口设备路径 |
| `uart` | `baud` | 460800 | 波特率 |

---

## 4. 坐标系与模型 (TF & URDF)

### 4.1 TF 树结构

```
map (由 SLAM 发布)
 └─ odom (由 EKF 发布)
     └─ base_link (机器人基座)
         ├─ left_wheel_Link (左驱动轮)
         ├─ right_wheel_Link (右驱动轮)
         ├─ radar_Link (激光雷达)
         └─ spine_Link (躯干)
             ├─ camera_Link (相机)
             ├─ left_arm_Link (左机械臂)
             └─ right_arm_Link (右机械臂)
```

### 4.2 关键坐标变换

#### **动态变换 (Dynamic TF)**
- **map -> odom**：由 SLAM Toolbox 发布，表示地图与里程计坐标系的关系
- **odom -> base_link**：由 EKF 节点发布，表示机器人在里程计坐标系中的位姿

#### **静态变换 (Static TF)**
- **base_link -> radar_Link**：雷达安装位置
  - 平移：`xyz="0.029032 0 0.0532"`
  - 旋转：`rpy="0 0 0"`

- **base_link -> left_wheel_Link**：左轮安装位置
  - 平移：`xyz="0.055 0.038192 0.021"`

- **base_link -> right_wheel_Link**：右轮安装位置
  - 平移：`xyz="-0.055 -0.038192 0.021"`

- **spine_Link -> camera_Link**：相机安装位置
  - 平移：`xyz="0.069858 0.024995 0.11868"`

### 4.3 URDF 模型说明

机器人模型由 SolidWorks 导出，包含以下主要部件：

- **base_link**：底盘基座，质量 0.447 kg
- **left_wheel_Link / right_wheel_Link**：连续旋转关节 (continuous)，质量 0.0167 kg
- **radar_Link**：固定关节 (fixed)，质量 0.0135 kg
- **spine_Link**：旋转关节 (revolute)，质量 0.477 kg
- **camera_Link**：固定关节 (fixed)，质量 0.00395 kg
- **left_arm_Link / right_arm_Link**：旋转关节 (revolute)，质量 0.0563 kg

所有连杆均包含碰撞模型 (collision) 和视觉模型 (visual)，Mesh 文件位于 `butter_robot_description/meshes/` 目录。

---

## 5. 启动与运行 (Launch & Run)

### 5.1 启动文件说明

#### **all_launch.launch.py (系统总启动)**
启动所有核心子系统，包括：
1. UART 串口通信节点
2. EKF 里程计融合节点
3. 音频交互模块 (ASR + RKLLM + TTS)
4. 机器人模型描述 (URDF + TF)
5. Foxglove Bridge (Web 可视化，端口 8765)
6. 视觉处理管道 (Camera + Inference + Streamer，零拷贝容器)

#### **slam.launch.py (SLAM 建图)**
启动 SLAM 建图功能：
1. 激光滤波节点 (laser_filter)
2. SLAM Toolbox 异步建图节点

#### **localization.launch.py (定位模式)**
使用已有地图进行定位

#### **ekf_odom.launch.py (里程计融合)**
启动 EKF 节点，融合轮式里程计和 IMU

### 5.2 运行指令

#### **编译工作空间**
```bash
cd /home/k/hy_linux/nfs/hy_ros
colcon build --symlink-install
```

#### **加载环境变量**
```bash
source install/setup.bash
```

#### **启动完整系统**
```bash
ros2 launch all_launch all_launch.launch.py
```

#### **单独启动 SLAM 建图**
```bash
ros2 launch hy_slam slam.launch.py
```

#### **单独启动里程计融合**
```bash
ros2 launch ekf_odom ekf_odom.launch.py
```

#### **查看 TF 树**
```bash
ros2 run tf2_tools view_frames
```

#### **查看话题列表**
```bash
ros2 topic list
```

#### **监控话题数据**
```bash
# 查看激光雷达数据
ros2 topic echo /scan

# 查看融合后的里程计
ros2 topic echo /odometry/filtered

# 查看电机状态
ros2 topic echo /uart/motor_status
```

#### **访问 Foxglove 可视化**
在浏览器中打开 Foxglove Studio，连接到 `ws://机器人IP:8765`

#### **访问 RTSP 视频流**
使用 VLC 或其他播放器打开：`rtsp://机器人IP:8554/camera`

---

## 6. 常见问题 (Troubleshooting)

### 6.1 TF 树相关问题

**现象**：RViz 中雷达数据显示异常或机器人模型不显示

**排查步骤**：
```bash
# 1. 检查 TF 树是否完整
ros2 run tf2_tools view_frames

# 2. 查看 TF 变换关系
ros2 run tf2_ros tf2_echo base_link radar_Link

# 3. 检查是否有 TF 断裂
ros2 topic echo /tf
```

**常见原因**：
- butter_robot_description 节点未启动
- URDF 文件路径错误
- 坐标系名称不匹配

### 6.2 里程计融合问题

**现象**：机器人定位漂移严重

**排查步骤**：
```bash
# 1. 检查 EKF 输入源
ros2 topic hz /uart/wheel_raw
ros2 topic hz /uart/imu_data

# 2. 查看 EKF 输出
ros2 topic echo /odometry/filtered

# 3. 检查 EKF 配置
cat src/ekf_odom/config/ekf.yaml
```

**解决方案**：
- 确认轮式里程计和 IMU 数据发布频率正常 (>10Hz)
- 调整 EKF 配置文件中的过程噪声协方差
- 检查 IMU 数据的坐标系是否正确

### 6.3 串口通信问题

**现象**：无法与 STM32 通信，没有数据上报

**排查步骤**：
```bash
# 1. 检查串口设备是否存在
ls -l /dev/ttyS3

# 2. 检查串口权限
sudo chmod 666 /dev/ttyS3

# 3. 测试串口通信
ros2 topic echo /uart/motor_status
```

**常见原因**：
- 串口设备路径错误
- 波特率不匹配 (应为 460800)
- 串口权限不足
- STM32 固件未正常运行

### 6.4 SLAM 建图问题

**现象**：SLAM 建图效果差，地图扭曲

**排查步骤**：
```bash
# 1. 检查激光雷达数据
ros2 topic echo /scan

# 2. 检查激光滤波是否正常
ros2 topic echo /scan_filtered

# 3. 查看 SLAM 参数配置
cat src/hy_slam/config/mapper_params_online_async.yaml
```

**解决方案**：
- 确认激光雷达数据正常，无大量噪点
- 检查激光滤波配置，确保过滤掉机器人自身
- 调整 SLAM 参数，如分辨率、匹配阈值等
- 确保机器人运动平稳，避免急转急停

### 6.5 视觉系统问题

**现象**：RTSP 视频流无法播放或延迟高

**排查步骤**：
```bash
# 1. 检查视觉管道各节点状态
ros2 node list | grep rk

# 2. 检查图像话题
ros2 topic hz /yolo/image_raw
ros2 topic hz /yolo/image_infer

# 3. 检查容器日志
ros2 node info /butter_vision_container
```

**解决方案**：
- 确认相机设备正常 (device_id: 0)
- 检查 RKNN 模型文件是否存在
- 确认 RTSP 服务器正常运行 (端口 8554)
- 降低分辨率或帧率以减少延迟

---

## 7. 系统特性与技术亮点 (Features & Highlights)

### 7.1 硬件加速与性能优化

#### **RK3588 NPU 加速**
- YOLOv8 模型通过 RKNN 工具链转换，利用 NPU 进行推理
- 推理性能：1920x1080 分辨率下可达 30fps
- 功耗优化：相比 CPU 推理降低 70% 功耗

#### **零拷贝通信 (Zero-Copy)**
- 视觉处理管道使用 `ComposableNodeContainer`
- 启用 `use_intra_process_comms` 进程内通信
- 避免图像数据在节点间的内存拷贝，降低延迟

### 7.2 多传感器融合

#### **EKF 里程计融合**
- 融合轮式里程计和 IMU 数据
- 线速度来自轮式编码器，角速度和姿态来自 IMU
- 2D 模式优化，忽略 Z 轴和 Roll/Pitch
- 50Hz 高频更新，提供平滑的定位输出

#### **激光数据滤波**
- 使用 `LaserScanBoxFilter` 过滤机器人自身遮挡
- 过滤区域：base_link 后方车身区域
- 避免将自身结构误认为障碍物

### 7.3 多模态交互

#### **语音交互**
- 实时语音识别 (ASR)
- 本地大语言模型推理 (RKLLM)
- 语音合成输出 (TTS)
- 完整的语音对话闭环

#### **多种控制方式**
- 键盘控制 (keyboard_control)
- 游戏手柄控制 (Joy)
- 语音指令控制
- 远程 Web 控制 (Foxglove)

### 7.4 远程监控与调试

#### **Foxglove Bridge**
- WebSocket 端口：8765
- 支持实时话题订阅和发布
- 支持参数动态调整
- 支持服务调用和连接图查看

#### **RTSP 视频流**
- 实时视频推流，端口 8554
- H.264 硬件编码，低延迟
- 支持多客户端同时观看

---

## 8. 开发建议 (Development Guidelines)

### 8.1 添加新传感器

1. 在 `src/` 目录下创建新的 ROS2 包
2. 实现传感器驱动节点
3. 在 `all_launch.launch.py` 中添加启动配置
4. 如需融合到里程计，修改 `ekf.yaml` 配置

### 8.2 调整 PID 参数

通过发布 `/uart/motor_control` 话题实时调整：
```bash
ros2 topic pub /uart/motor_control pid_debug_interfaces/msg/MotorControl \
  "{left_kp: 1.0, left_ki: 0.1, left_kd: 0.01, \
    right_kp: 1.0, right_ki: 0.1, right_kd: 0.01}"
```

### 8.3 更换 YOLO 模型

1. 使用 RKNN-Toolkit2 转换新模型
2. 将 `.rknn` 文件放置到 `/opt/rknn-toolkit2-lite/`
3. 修改 `all_launch.launch.py` 中的 `model_path` 参数
4. 重启视觉处理管道

### 8.4 性能优化建议

- **降低 CPU 占用**：调整节点发布频率，避免不必要的高频更新
- **降低内存占用**：使用 `ComposableNode` 合并相关节点
- **降低网络带宽**：压缩图像话题，使用 `compressed_image_transport`
- **提高实时性**：使用 ROS2 的 QoS 策略，设置合适的可靠性和历史深度

---

## 9. 总结 (Summary)

黄油机器人系统是一个功能完整的 ROS2 移动机器人平台，具备以下核心能力：

✅ **感知能力**：2D 激光雷达、IMU、轮式里程计、视觉识别
✅ **定位建图**：EKF 多传感器融合、SLAM 在线建图
✅ **智能交互**：语音识别、大语言模型、语音合成
✅ **视觉处理**：硬件加速的 YOLO 目标检测、RTSP 视频流
✅ **远程监控**：Foxglove Web 可视化、多种控制方式
✅ **性能优化**：零拷贝通信、NPU 加速、低延迟设计

系统采用模块化设计，各功能包职责清晰，易于扩展和维护。通过合理的传感器融合和硬件加速，实现了高性能、低功耗的机器人控制系统。

---

**文档版本**：v1.0
**生成日期**：2026-01-20
**维护者**：黄油机器人开发团队

